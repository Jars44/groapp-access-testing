# Test Plan: {Feature Name}

> Generated by Lead Architect. Each todo is independently verifiable by a human.

## Reference

| Field                | Value                                                     |
| -------------------- | --------------------------------------------------------- |
| **Feature**          | {feature name}                                            |
| **Source**           | {PRD / user story / AC link}                              |
| **Source route**     | `{sourceDir}/src/features/{feature}/presentation/routes/` |
| **API base**         | `{sourceDir}/src/features/{feature}/infrastructure/api/`  |
| **Test spec target** | `src/tests/specs/{feature}/{file}.spec.ts`                |
| **POM target**       | `src/tests/pages/{feature}/{file}.page.ts`                |

---

## Test Cases

### TC-{id}: {short description}

**Type:** `happy path` | `error` | `edge case` | `validation`

**Route:** `{target URL}`

**Given (precondition):**

- User is logged in as {role}
- Navigate to {route}
- {data already exists in system}

**When (action):**

1. {step}
2. {step}

**Then (expected):**

- {expected outcome}
- {expected UI state}
- {expected API call}

**Test data:**

```typescript
{test data payload or factory call}
```

**Selectors used:**

- `{selector1}` — `getByRole('button', { name: '...' })`
- `{selector2}` — `getByTestId('...')`

**Verification criteria (human checklist):**

- [ ] Form renders with correct labels
- [ ] Input accepts valid data
- [ ] Submit triggers correct API payload
- [ ] Success state shows correct message
- [ ] Error state shows correct message
- [ ] Navigation after action is correct
- [ ] No console errors

---

### TC-{id}: {short description}

... (repeat for each scenario)

---

## Coverage Summary

| Scenario | Status                                         | Verified by |
| -------- | ---------------------------------------------- | ----------- |
| TC-001   | [ ] not started / [x] implemented / [v] passed |             |
| TC-002   | [ ] not started / [x] implemented / [v] passed |             |

**Gaps:**

- {edge cases intentionally skipped with reason}
- {known limitations}

**Data dependencies:**

- {test user must have specific permissions}
- {test data must exist: specific company, workspace, etc.}
